# Дадата: API-ключ для подсказок адресов (suggest/address).
# Если не задан, используется значение по умолчанию из src/api/dadata.ts.
# Секретный ключ НЕ нужен: он требуется только серверному методу clean/address,
# который из браузера не вызывается.
VITE_DADATA_TOKEN=
